DATA
│
└── root: [               <-- array containing a single root object
      ┌───────────────────────────────────────────────────────────────────────┐
      │ {                                                                     }│
      │ name: string, the identifier of the node (e.g., "root")                │
      │ children: array of cluster objects, each representing a top‑level      │
      │           group (e.g., "Cluster A", "Cluster B")                     │
      │   [                                                                   │
      │     {                                                                 │
      │       name: string, name of the cluster (e.g., "Cluster A")         │
      │       children: array of leaf‑node objects, each representing a   │
      │                leaf node within the cluster                         │
      │       [                                                               │
      │         { name: string, name of the leaf node (e.g., "Node 1") }       │
      │         { name: string, ... }                                         │
      │         ...                                                          │
      │       ]                                                               │
      │     },                                                                │
      │     { … }   // additional clusters (e.g., "Cluster B")               │
      │   ]                                                                   │
      │ links: array of link objects, defining connections between      │
      │        leaf nodes across clusters                                 │
      │   [                                                                   │
      │     { source: number, index of source node (0‑based) },               │
      │     { target: number, index of target node (0‑based) }                │
      │     // one object per link (e.g., {source:0, target:5})                │
      │   ]                                                                   │
      └───────────────────────────────────────────────────────────────────────┘

Explanation of fields:
- name – string, categorical identifier for each node (root, clusters, or leaf nodes).
- children – array, hierarchical nesting of clusters and leaf nodes.
- links – array of objects, each with numeric source and target indices that reference leaf‑node positions in the flattened node list (used by the bundle transform).
- The chart also expects derived fields x, y, and path (generated by the hierarchy, cluster, and bundle transforms) for positioning and drawing the bundled edges.